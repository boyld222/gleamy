import Reveal from "../../Reveal";
import clsx from "clsx";

interface Props {
  title?: string;
}

export default function AboutSection({ title }: Props) {
  return (
    <div className="relative w-full h-screen overflow-hidden bg-black text-white pl-[200px] pt-[120px]">
      hihi
    </div>
  );
}
